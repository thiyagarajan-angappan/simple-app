import React from 'react';
import { Button } from '@material-ui/core';

const SAButton = () => {
    return (
        <Button xs={3} type="submit" variant="contained" color="primary">Login</Button>
    )
}

export default SAButton;